{
  "permissions": {
    "allow": [
      "Bash(npm install *)",
      "Bash(npm view *)",
      "Bash(npm run *)",
      "Bash(npm start *)",
      "Bash(tee /tmp/fngn-server.log)",
      "mcp__Claude_Preview__preview_start",
      "PowerShell(try { $r = Invoke-WebRequest -Uri \"http://localhost:3000\" -UseBasicParsing -TimeoutSec 10; \"HTTP \" + $r.StatusCode + \" len=\" + $r.Content.Length } catch { \"ERR: \" + $_.Exception.Message })"
    ]
  }
}
